/**
 * Created by coreybrown on 3/5/15.
 */
ftEDuApp.controller("videoPlayer",function(tCards,$scope,$rootScope,ftData,$sce){
    $scope.selected = function(_o)
    {
        console.log('clicked',_o);
    };
});